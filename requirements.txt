# Used by app.py and Pneumonia-detction.ipynb
# (google.colab is preinstalled on Colab and is only needed for the Drive mount)
tensorflow==2.20.0   # same version the model was trained/saved with
streamlit>=1.36
numpy>=1.26
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
pillow>=10.0
jupyter
